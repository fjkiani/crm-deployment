# =============================================================================
# Dockerfile.frappe
#
# This Dockerfile simply pulls the pre-built image from GHCR.
# The actual build happens in GitHub Actions (.github/workflows/build-frappe.yml).
# This avoids OOM issues during Render's Docker build.
# =============================================================================

FROM ghcr.io/fjkiani/crm-frappe:main
