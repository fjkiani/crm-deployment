{
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd frontend && yarn install --ignore-engines",
    "dev": "cd frontend && yarn dev",
    "build": "cd frontend && yarn build",
    "production": "cd frontend && yarn production"
  }
}
