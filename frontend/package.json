{
  "name": "crm-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.error('fc-app-only: Vue source lives on main'); process.exit(1)\"",
    "build": "node scripts/build.mjs",
    "production": "node scripts/build.mjs",
    "copy-html-entry": "cp ../crm/public/frontend/index.html ../crm/www/crm.html",
    "serve": "node -e \"console.error('fc-app-only: no Vite serve'); process.exit(1)\""
  }
}
